import * as React from 'react' ;


const admDrawer = () => {
    const [open, setOpen] = React.useState(false);
  
    const  = (newOpen: boolean) => () => {
      setOpen(newOpen);
    };

const admDrawer = {
    
}
}
export admDrawer